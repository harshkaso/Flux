import dearpygui.dearpygui as dpg
from math import sin, cos, pi

class Vector2D:

    def __init__(self, x, y, angle=0, len=5) -> None:
        self.x = x
        self.y = y
        self.angle = angle

    def heading()

